//! Manifests describe the validated and more rust native interpretation of the
//! [`wasserxr::definitions`]. They are the actual handles that are stored and used internally.

pub mod components;
pub mod fields;
pub mod plugins;
